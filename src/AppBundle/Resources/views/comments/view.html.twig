{% extends 'AppBundle::base.html.twig' %}
{% block body %}
    <h2>Komentarze dla posta: {{ comment.post.topic }}, user: {{ comment.user.username }}</h2>
    <div class="comment-content">
        <h4>Treść:</h4>
        <p>{{ comment.content }}</p>
    </div>
{% endblock %}